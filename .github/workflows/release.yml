name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-macos-arm64:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: brew install cmake curl 2>/dev/null || true

      - name: Build
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=OFF
          cmake --build build -j$(sysctl -n hw.ncpu)

      - name: Package
        run: |
          mkdir -p dist/calendly
          cp build/calendly dist/calendly/
          cp LICENSE dist/calendly/
          cd dist && tar czf calendly-darwin-arm64.tar.gz calendly/

      - uses: actions/upload-artifact@v4
        with:
          name: calendly-darwin-arm64
          path: dist/calendly-darwin-arm64.tar.gz

  build-macos-x86_64:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: brew install cmake curl 2>/dev/null || true

      - name: Build
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=OFF \
            -DCMAKE_OSX_ARCHITECTURES=x86_64
          cmake --build build -j$(sysctl -n hw.ncpu)

      - name: Package
        run: |
          mkdir -p dist/calendly
          cp build/calendly dist/calendly/
          cp LICENSE dist/calendly/
          cd dist && tar czf calendly-darwin-x86_64.tar.gz calendly/

      - uses: actions/upload-artifact@v4
        with:
          name: calendly-darwin-x86_64
          path: dist/calendly-darwin-x86_64.tar.gz

  build-linux-x86_64:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake libcurl4-openssl-dev

      - name: Build
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=OFF \
            -DCMAKE_EXE_LINKER_FLAGS="-static-libgcc -static-libstdc++"
          cmake --build build -j$(nproc)

      - name: Package
        run: |
          mkdir -p dist/calendly
          cp build/calendly dist/calendly/
          cp LICENSE dist/calendly/
          cd dist && tar czf calendly-linux-x86_64.tar.gz calendly/

      - uses: actions/upload-artifact@v4
        with:
          name: calendly-linux-x86_64
          path: dist/calendly-linux-x86_64.tar.gz

  release:
    needs: [build-macos-arm64, build-macos-x86_64, build-linux-x86_64]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Generate checksums
        run: cd artifacts && sha256sum *.tar.gz > sha256sums.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/*.tar.gz
            artifacts/sha256sums.txt
